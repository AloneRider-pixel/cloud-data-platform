ecommerce:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: 5432
      user: data_engineer
      password: data_password
      dbname: data_platform
      schema: staging
      threads: 4
    
    prod:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: 5432
      user: data_engineer
      password: data_password
      dbname: data_platform
      schema: analytics
      threads: 8
